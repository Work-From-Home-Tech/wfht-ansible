---
galaxy_info:
  author: WFHT
  description: Installs and configures a Docker Swarm cluster
  license: MIT
  min_ansible_version: 2.9
  platforms:
    - name: Ubuntu
      versions:
        - all
    - name: Debian
      versions:
        - all
    - name: EL
      versions:
        - 7
        - 8
        - 9
  galaxy_tags:
    - docker
    - swarm
    - container
    - orchestration

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above, if you add dependencies
  # to this list.
  # - install-docker
